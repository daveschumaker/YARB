var expect = require('chai').expect;
var should = require('chai').should();

describe('index.js', function() {
    it('first test should resolve to true', function() {
        var test = true;
        test.should.equal(true);
    });
});
